<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox spacing="14" xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.controller.student.StudentChangePasswordController"
      style="-fx-padding: 12;">

    <Label text="ĐỔI MẬT KHẨU (OTP)"
           style="-fx-font-size: 22; -fx-font-weight: bold; -fx-text-fill: #004D40;" />

    <VBox spacing="10" style="-fx-border-color: #E0E0E0; -fx-border-width: 1; -fx-padding: 14; -fx-background-color: white;">
        <Label fx:id="lblInfo" text="Bấm Gửi OTP để nhận mã qua email đã lưu trong hồ sơ."
               style="-fx-text-fill: #333;" wrapText="true"/>

        <HBox spacing="10">
            <Button fx:id="btnSendOtp" text="📩 Gửi OTP" onAction="#sendOtpHandle"
                    style="-fx-background-color: #00796B; -fx-text-fill: white; -fx-background-radius: 6;"/>
            <Label fx:id="lblSentTo" text="" style="-fx-text-fill: #555;"/>
        </HBox>

        <Separator/>

        <GridPane hgap="12" vgap="10">
            <columnConstraints>
                <ColumnConstraints prefWidth="140"/>
                <ColumnConstraints hgrow="ALWAYS"/>
            </columnConstraints>

            <Label text="OTP:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
            <TextField fx:id="tfOtp" promptText="Nhập 6 số" GridPane.rowIndex="0" GridPane.columnIndex="1"/>

            <Label text="Mật khẩu mới:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
            <PasswordField fx:id="pfNew" promptText=">= 6 ký tự" GridPane.rowIndex="1" GridPane.columnIndex="1"/>

            <Label text="Nhập lại:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
            <PasswordField fx:id="pfConfirm" promptText="Nhập lại mật khẩu mới" GridPane.rowIndex="2" GridPane.columnIndex="1"/>
        </GridPane>

        <HBox alignment="CENTER_RIGHT">
            <Button text="✅ Đổi mật khẩu" onAction="#changePasswordHandle"
                    style="-fx-background-color: #1E88E5; -fx-text-fill: white; -fx-background-radius: 6;"/>
        </HBox>
    </VBox>
</VBox>
